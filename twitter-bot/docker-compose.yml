version: '3.8'

services:
  twitter-bot:
    build: .
    container_name: helm-analytics-bot
    env_file: .env
    environment:
      - TWITTER_REDIRECT_URI=http://localhost:8080/twitter/callback
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    restart: unless-stopped
